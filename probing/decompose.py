"""Component-level decomposition of probing signal.

Runs probes on per-component activations (per-head, per-MLP layer) to
attribute failure prediction to specific circuits rather than the residual
stream wholesale.

Status: NOT IMPLEMENTED — Phase 2. See spec.md.
"""
